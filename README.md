🎮 BePex Unity ClientUnity Clean Code & Design Patterns 기반의 확장 가능한 퀘스트(이벤트) 센터 시스템 및 관리자 프로그램본 프로젝트는 이벤트 데이터의 생성, 추적, 보상 지급, 저장에 이르는 전체 라이프사이클을 단방향 데이터 흐름(MVVM)과 순수 의존성 주입(Pure DI)으로 제어하는 클라이언트 시스템입니다.📑 목차📌 프로젝트 정보 및 실행 방법⚙️ 시스템 아키텍처 및 도메인 설계🚀 시스템 확장 가이드 (기능 추가)📐 주요 설계 의도 및 최적화 포인트⏳ 작업 로그 및 AI 활용 내역1. 📌 프로젝트 정보 및 실행 방법Unity 버전 : 6.3.16f1로컬 세이브 연동 : 어드민과 인게임 앱은 persistentDataPath를 공유하여 로컬 파일 기반으로 데이터를 상호 교환합니다.🏃‍♂️ 씬(Scene) 실행 방법① 인게임 이벤트 센터 (SampleScene)Assets/_Game/Scenes/Inagme.unity 씬을 열고 Play(▶) 합니다.💡 테스트/디버그 모드: [System]/EventSceneInitializer 오브젝트 인스펙터에서 m_useDebugMode를 체크하면 우측 하단에 치트 조작 패널이 활성화됩니다.② 이벤트 관리자 어드민 (EventAdminScene)Assets/_Game/Scenes/EventAdminScene.unity 씬을 열고 Play(▶) 합니다.⚠️ 주의사항: 이벤트를 추가/편집 후 [로컬 파일 저장]을 누르면 로컬 JSON이 즉시 변경됩니다. [Firebase 서버 배포] 클릭 시 비동기 업로드(1.5초 지연 시뮬레이션)가 진행됩니다.📦 Standalone 분리 빌드 (빌드 자동화)Unity 에디터 상단 메뉴 BePex > Build 를 통해 씬별 단독 실행형 바이너리를 자동 빌드할 수 있습니다.Build Admin Standalone: Builds/Admin/ 폴더에 어드민 생성Build Ingame Standalone: 어드레서블 자산 갱신 후 Builds/Ingame/ 폴더에 인게임 생성Build All Standalone: 어드민 ➔ 인게임 순차 자동 빌드2. ⚙️ 시스템 아키텍처 및 도메인 설계2.1 아키텍처 다이어그램 (Pure DI & MVVM)전역 싱글톤을 배제하고, 각 씬의 Composition Root(Initializer)가 씬 로드 시 도메인, 뷰모델, 서비스를 한 번만 생성자 주입하여 결합도를 낮춥니다.flowchart TD
    subgraph Unity_Admin_Scene ["1. 이벤트 관리자 씬 (EventAdminScene)"]
        A1[EventAdminView] -->|바인딩/명령| A2[EventAdminViewModel]
        A2 -->|전략 주입| A3[MockFirebaseUploadService]
        A2 -->|로컬 I/O| A4[event_table.json 생성]
    end

    subgraph Unity_Runtime_Client ["2. 라이브 클라이언트 (Ingame)"]
        C1[EventSceneInitializer] -->|Addressables 로드| C2[JSON 역직렬화]
        C2 -->|생성자 주입| C3[EventModel 도메인]
        C3 -->|Action 바인딩| C4[UI Views]
    end

    A4 -.->|빌드 파이프라인| C1
2.2 주요 도메인 클래스 매핑 (평가 규정 기준)사전 과제에서 요구하는 핵심 책임 요소들이 본 프로젝트에서 어떻게 구현되었는지 매핑한 표입니다.핵심 책임실제 구현 클래스주요 역할EventManagerEventModel이벤트 로드, 조건 검사, 진행 상태 제어 및 상태 변화 전파 (Core)EventTrackerIQuestCondition 계열퀘스트별 달성 조건 관리 및 진행 수치 검증/추적 전략RewardSystemQuestRewardFactory조건 달성 시 보상 인스턴스를 생성하고 플레이어 자산에 적립AttendanceEventAttendanceQuestCondition24시간 가드 로직을 내장하여 일일 1회만 누적되는 출석 체크MissionEventStandardQuestCondition적 처치, 스테이지 클리어 등 단순 행동 값의 누적 및 비교 판정PointSystemPlayerRewardModel재화(EventPoint) 획득 및 교환을 딕셔너리로 관리하는 자산 시스템SaveSystemJsonSaveSystem플레이어 진행도 및 보상 DTO를 디스크 I/O로 영속 저장 (Newtonsoft 기반)EventUIEventListView 외View-ViewModel 단방향 바인딩을 준수하는 리액티브 UI 컴포넌트Model / DTO 계층: EventModel, QuestProgressModel, PlayerRewardModel(재화 딕셔너리), EventTableSO 등 순수 데이터와 비즈니스 로직.ViewModel 계층: EventListViewModel, RewardPopupViewModel 등 뷰와 모델 사이의 데이터/명령 중개.View 계층: EventListView, CurrencyHUDView 등 실질적인 렌더링 및 입력 처리.Condition (조건 전략): BaseQuestCondition, StandardQuestCondition 등 목표 달성 판정 로직.Reward (보상 전략): BaseQuestReward, GeneralQuestReward 등 보상 지급 실행 로직.Interfaces (규격): ISaveSystem, IQuestCondition, IFirebaseUploadService 등 결합도를 낮추는 통일 규격.Infrastructure (인프라): EventSceneInitializer(DI 조립), CachedSaveSystem(I/O 최적화).Factories: QuestConditionFactory, QuestRewardFactory 등 동적 객체 생성(리플렉션/폴백 지원).Editor: EventExtensionWindow 등 개발 생산성을 돕는 툴바 윈도우.3. 🚀 시스템 확장 가이드 (기능 추가)본 시스템은 데이터 기반(Data-driven) 팩토리 폴백 구조를 채택하여, 단순 비교 로직은 C# 코딩 없이 에디터 조작만으로 확장이 가능합니다.3.1 새로운 퀘스트 조건 추가 (예: 로그인 횟수)에디터 도구 위치: Tools > BePex > 이벤트 시스템 확장 도구A. 단순 카운트 비교 조건 (C# 코딩 ❌ - 권장)확장 대상: 이벤트 타입 선택식별자 입력 (예: LoginCount, 로그인 횟수)"C# 파일 추가 생성" 토글 OFF 후 생성 실행결과: 데이터 에셋만 생성되며, 런타임에 범용 StandardQuestCondition이 자동 매핑되어 처리합니다.B. 특수 가드 로직이 필요한 조건 (C# 코딩 ⭕️)위 설정에서 "C# 파일 추가 생성" 토글 ON 후 실행자동 생성된 GuildMissionQuestCondition.cs의 CanAddProgress를 오버라이드하여 시간 비교 등 특수 로직을 작성합니다.3.2 새로운 보상 타입 추가 (예: 루비 재화)A. 단순 가산형 재화 (C# 코딩 ❌ - 권장)확장 대상: 보상 타입 선택 후 식별자 입력 (예: Ruby)"C# 파일 추가 생성" 토글 OFF 후 생성 실행결과: 런타임에 범용 GeneralQuestReward가 동적 매핑되어 PlayerRewardModel.AddCurrency("Ruby")를 안전하게 실행합니다.B. 특수 연출(우편함 연동 등) 보상 (C# 코딩 ⭕️)토글 ON으로 C# 파일을 생성한 후 Grant(PlayerRewardModel) 내부에 커스텀 로직을 작성합니다.3.3 인게임 행동에 따른 '포인트 적립' 구현 예시인게임 컨트롤러(예: 스테이지 클리어)에서 주입받은 PlayerRewardModel을 호출하기만 하면 상단 UI까지 자동으로 갱신됩니다.public void func_OnStageClear(int stageId)
{
    int rewardPoint = 15; // 획득 포인트 산출
    
    if (m_playerRewardModel != null)
    {
        // 1. 모델에 재화 가산 (내부적으로 저장 로직 트리거)
        m_playerRewardModel.AddCurrency("EventPoint", rewardPoint);
    }
    // 2. ViewModel이 모델의 이벤트를 구독 중이므로, CurrencyHUDView가 즉시 갱신됨
}
4. 📐 주요 설계 의도 및 최적화 포인트⚡ Zero-Allocation (GC 억제) 전면 적용기존 갱신마다 리스트를 신규 할당하던 구조를 폐기하고, 캐시 버퍼(IReadOnlyList)를 재사용하는 GetActiveEventsNonAlloc 구조를 도입하여 런타임 GC 스파이크를 원천 차단했습니다.잦은 DateTime.TryParse 파싱 오버헤드를 막기 위해 DTO 내부에 Nullable 캐싱 필드를 적용했습니다.🔄 Newtonsoft.Json 기반 POCO 현대화 및 마이그레이션기존 JsonUtility의 Dictionary 직렬화 불가능 문제를 Newtonsoft.Json 도입으로 해결했습니다.[OnDeserialized] 역직렬화 수명 주기를 활용해, 레거시 세이브 파일 로드 시에도 에러 없이 최신 딕셔너리 포맷으로 자동 합산/복구하는 하위 호환성 마이그레이션 로직을 구현했습니다.🛡️ Awaitable 동시성 크래시 해결 및 안전성 확보Unity 6의 Awaitable 단 1회 await 제약(Detached State)에 따른 크래시를 방지하기 위해 HashSet 기반 락(Lock) 상태 관리와 프레임 슬롯 폴링 방식을 고안하여 비동기 세이브의 동시성을 제어했습니다.뷰(View)의 async void 진입점에 try-catch(OperationCanceledException) 래핑을 적용해 씬 소멸 시 발생하는 누수와 에러를 차단했습니다.🧩 아키텍처 확장성 (OCP & DIP)ISaveSystem과 IFirebaseUploadService 인터페이스로 입출력을 추상화하여 추후 백엔드 교체가 자유롭습니다.데코레이터 패턴을 적용한 RetrySaveSystemDecorator를 DI 조립하여, 저장 실패 시 지수 백오프(Exponential Backoff) 재시도를 통해 로컬/원격 저장의 회복력을 높였습니다.5. ⏳ 작업 로그 및 AI 활용 내역🕒 Time Log (총 48.5시간)12.0H : 설계, 다이어그램 및 아키텍처 문서화15.5H : 이벤트 시스템 OCP 구조 개편 및 팩토리 로직 구현10.0H : 리액티브 UI 연동 및 비동기(Awaitable) 성능 튜닝11.0H : Newtonsoft.Json 적용, 마이그레이션 및 유닛 테스트 보완🤖 AI Usage (Antigravity Agent / Opus, Gemini 계열)주요 활용 범위:PlayerRewardModel Dictionary 직렬화 변환 및 하위 호환 마이그레이션 설계 검토.CachedSaveSystem 내 Awaitable Detached State 크래시 방지를 위한 HashSet 프레임 대기 기작 아이디어 차용.C# 깡통 클래스를 줄이는 팩토리 폴백(Fallback) 라우팅 로직 정교화.지수 백오프 기반 RetrySaveSystemDecorator 구조 설계.검증 방법:AI의 제안은 즉시 적용하지 않고, Unity Test Runner 내 EditMode(24종) / PlayMode(6종) 총 30개의 유닛/통합 테스트 시나리오를 100% Passed 하여 논리적 결함을 교차 검증했습니다.